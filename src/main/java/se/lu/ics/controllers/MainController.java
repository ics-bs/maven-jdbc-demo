package se.lu.ics.controllers;

public class MainController {

    /*
     * MainController has no behavior at the moment as
     * DepartmentController and EmployeeController handle
     * All events of the nested fxml files
     * 
     * If we were to add additional elements to  Main.fxml
     * apart from the nexted fxml files, we would need to
     * add the appropriate event handlers here
     * 
     */

}
